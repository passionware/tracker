# Deprecated — use repo-root `.cursor/rules/*.mdc`

Project Cursor rules live in **`/.cursor/rules/`** (see `tracker-*.mdc`). This file is kept as a pointer so anyone opening `src/.cursorrules` knows where rules moved.
